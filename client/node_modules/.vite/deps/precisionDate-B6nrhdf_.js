import { a as IsWindowObjectExist } from "./domManagement-DD_-GDGa.js";
//#region ../../node_modules/.pnpm/@babylonjs+core@9.25.0/node_modules/@babylonjs/core/Misc/precisionDate.js
/**
* Class containing a set of static utilities functions for precision date
*/
var PrecisionDate = class {
	/**
	* Gets either window.performance.now() if supported or Date.now() else
	*/
	static get Now() {
		if (IsWindowObjectExist() && window.performance && window.performance.now) return window.performance.now();
		return Date.now();
	}
};
//#endregion
export { PrecisionDate as t };
