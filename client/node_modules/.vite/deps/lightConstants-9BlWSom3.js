import { f as InvertMatrixToRef, g as MultiplyMatricesToRef, o as Vector3, t as Matrix } from "./math.vector.pure-BrntvQ1d.js";
import { t as UniformBuffer } from "./uniformBuffer-gPT9G4tb.js";
import { s as Effect } from "./abstractEngine.pure-CImT488Y.js";
//#region ../../node_modules/.pnpm/@babylonjs+core@9.25.0/node_modules/@babylonjs/core/Materials/floatingOriginMatrixOverrides.js
var TempFinalMat = new Matrix();
var TempMat1 = new Matrix();
var TempMat2 = new Matrix();
/**
* When rendering, each scene will reset this to ensure the correct floating origin offset is when overriding the below functions
*/
var FloatingOriginCurrentScene = {
	getScene: () => void 0,
	eyeAtCamera: true
};
function OffsetWorldToRef(offset, world, ref) {
	const refArray = ref.asArray();
	const worldArray = world.asArray();
	for (let i = 0; i < 16; i++) refArray[i] = worldArray[i];
	refArray[12] -= offset.x;
	refArray[13] -= offset.y;
	refArray[14] -= offset.z;
	ref.markAsUpdated();
	return ref;
}
function GetFullOffsetView(offset, viewMatrix, ref) {
	InvertMatrixToRef(viewMatrix, TempMat1);
	OffsetWorldToRef(offset, TempMat1, TempMat2);
	InvertMatrixToRef(TempMat2, ref);
	return ref;
}
function OffsetViewToRef(offset, view, ref) {
	if (!FloatingOriginCurrentScene.eyeAtCamera) return GetFullOffsetView(offset, view, ref);
	const refArray = ref.asArray();
	const viewArray = view.asArray();
	for (let i = 0; i < 16; i++) refArray[i] = viewArray[i];
	refArray[12] = 0;
	refArray[13] = 0;
	refArray[14] = 0;
	ref.markAsUpdated();
	return ref;
}
function OffsetViewProjectionToRef(offset, view, projection, ref) {
	MultiplyMatricesToRef(OffsetViewToRef(offset, view, ref), projection, ref);
	return ref;
}
function OffsetClipPlaneToRef(offset, plane, ref) {
	ref.normal.copyFrom(plane.normal);
	ref.d = plane.d + Vector3.Dot(plane.normal, offset);
	return ref;
}
function GetOffsetTransformMatrices(offset, viewMatrices, projectionMatrices, length, resultArray) {
	for (let cascadeIndex = 0; cascadeIndex < length; ++cascadeIndex) {
		GetFullOffsetViewProjectionToRef(offset, viewMatrices[cascadeIndex], projectionMatrices[cascadeIndex], TempMat1);
		TempMat1.copyToArray(resultArray, cascadeIndex * 16);
	}
	return resultArray;
}
function OffsetWorldViewToRef(offset, worldView, view, ref) {
	InvertMatrixToRef(view, TempMat1);
	MultiplyMatricesToRef(worldView, TempMat1, TempMat2);
	OffsetWorldToRef(offset, TempMat2, TempMat1);
	OffsetViewToRef(offset, view, TempMat2);
	MultiplyMatricesToRef(TempMat1, TempMat2, ref);
	return ref;
}
function GetFullOffsetViewProjectionToRef(offset, viewMatrix, projectionMatrix, ref) {
	GetFullOffsetView(offset, viewMatrix, TempMat2);
	MultiplyMatricesToRef(TempMat2, projectionMatrix, ref);
	return ref;
}
function OffsetWorldViewProjectionToRef(offset, worldViewProjection, viewProjection, view, projection, ref) {
	InvertMatrixToRef(viewProjection, TempMat1);
	MultiplyMatricesToRef(worldViewProjection, TempMat1, TempMat2);
	OffsetWorldToRef(offset, TempMat2, TempMat1);
	OffsetViewProjectionToRef(offset, view, projection, TempMat2);
	MultiplyMatricesToRef(TempMat1, TempMat2, ref);
	return ref;
}
function GetOffsetMatrix(uniformName, mat) {
	const scene = FloatingOriginCurrentScene.getScene();
	if (!scene || TempFinalMat === mat) return mat;
	TempFinalMat.updateFlag = mat.updateFlag;
	const offset = scene.floatingOriginOffset;
	switch (uniformName) {
		case "world": return OffsetWorldToRef(offset, mat, TempFinalMat);
		case "view": return OffsetViewToRef(offset, mat, TempFinalMat);
		case "worldView": return OffsetWorldViewToRef(offset, mat, scene.getViewMatrix(), TempFinalMat);
		case "viewProjection": return OffsetViewProjectionToRef(offset, scene.getViewMatrix(), scene.getProjectionMatrix(), TempFinalMat);
		case "worldViewProjection": return OffsetWorldViewProjectionToRef(offset, mat, scene.getTransformMatrix(), scene.getViewMatrix(), scene.getProjectionMatrix(), TempFinalMat);
		default:
			if (uniformName.startsWith("u_")) {
				const lowercaseUniformName = uniformName.toLowerCase();
				if (lowercaseUniformName.startsWith("u_worldviewprojection")) return OffsetWorldViewProjectionToRef(offset, mat, scene.getTransformMatrix(), scene.getViewMatrix(), scene.getProjectionMatrix(), TempFinalMat);
				if (lowercaseUniformName.startsWith("u_viewprojection")) return OffsetViewProjectionToRef(offset, scene.getViewMatrix(), scene.getProjectionMatrix(), TempFinalMat);
				if (lowercaseUniformName.startsWith("u_worldview")) return OffsetWorldViewToRef(offset, mat, scene.getViewMatrix(), TempFinalMat);
				if (lowercaseUniformName.startsWith("u_world")) return OffsetWorldToRef(offset, mat, TempFinalMat);
				if (lowercaseUniformName.startsWith("u_view")) return OffsetViewToRef(offset, mat, TempFinalMat);
			}
			return mat;
	}
}
var UniformBufferInternal = UniformBuffer;
var EffectInternal = Effect;
var OriginalUpdateMatrixForUniform = UniformBufferInternal.prototype._updateMatrixForUniform;
var OriginalSetMatrix = Effect.prototype.setMatrix;
function ResetMatrixFunctions() {
	Effect.prototype.setMatrix = OriginalSetMatrix;
	EffectInternal._setMatrixOverride = void 0;
	UniformBufferInternal.prototype._updateMatrixForUniform = OriginalUpdateMatrixForUniform;
	UniformBufferInternal.prototype._updateMatrixForUniformOverride = void 0;
}
function OverrideMatrixFunctions() {
	EffectInternal.prototype._setMatrixOverride = OriginalSetMatrix;
	EffectInternal.prototype.setMatrix = function(uniformName, matrix) {
		this._setMatrixOverride(uniformName, GetOffsetMatrix(uniformName, matrix));
		return this;
	};
	UniformBufferInternal.prototype._updateMatrixForUniformOverride = OriginalUpdateMatrixForUniform;
	UniformBufferInternal.prototype._updateMatrixForUniform = function(uniformName, matrix) {
		this._updateMatrixForUniformOverride(uniformName, GetOffsetMatrix(uniformName, matrix));
	};
}
//#endregion
//#region ../../node_modules/.pnpm/@babylonjs+core@9.25.0/node_modules/@babylonjs/core/Lights/lightConstants.js
/** Defines the cross module constantsused by lights to avoid circular dependencies */
var LightConstants = class {
	/**
	* Sort function to order lights for rendering.
	* @param a First Light object to compare to second.
	* @param b Second Light object to compare first.
	* @returns -1 to reduce's a's index relative to be, 0 for no change, 1 to increase a's index relative to b.
	*/
	static CompareLightsPriority(a, b) {
		if (a.shadowEnabled !== b.shadowEnabled) return (b.shadowEnabled ? 1 : 0) - (a.shadowEnabled ? 1 : 0);
		return b.renderPriority - a.renderPriority;
	}
};
/**
* Falloff Default: light is falling off following the material specification:
* standard material is using standard falloff whereas pbr material can request special falloff per materials.
*/
LightConstants.FALLOFF_DEFAULT = 0;
/**
* Falloff Physical: light is falling off following the inverse squared distance law.
*/
LightConstants.FALLOFF_PHYSICAL = 1;
/**
* Falloff gltf: light is falling off as described in the gltf moving to PBR document
* to enhance interoperability with other engines.
*/
LightConstants.FALLOFF_GLTF = 2;
/**
* Falloff Standard: light is falling off like in the standard material
* to enhance interoperability with other materials.
*/
LightConstants.FALLOFF_STANDARD = 3;
/**
* If every light affecting the material is in this lightmapMode,
* material.lightmapTexture adds or multiplies
* (depends on material.useLightmapAsShadowmap)
* after every other light calculations.
*/
LightConstants.LIGHTMAP_DEFAULT = 0;
/**
* material.lightmapTexture as only diffuse lighting from this light
* adds only specular lighting from this light
* adds dynamic shadows
*/
LightConstants.LIGHTMAP_SPECULAR = 1;
/**
* material.lightmapTexture as only lighting
* no light calculation from this light
* only adds dynamic shadows from this light
*/
LightConstants.LIGHTMAP_SHADOWSONLY = 2;
/**
* Each light type uses the default quantity according to its type:
*      point/spot lights use luminous intensity
*      directional lights use illuminance
*/
LightConstants.INTENSITYMODE_AUTOMATIC = 0;
/**
* lumen (lm)
*/
LightConstants.INTENSITYMODE_LUMINOUSPOWER = 1;
/**
* candela (lm/sr)
*/
LightConstants.INTENSITYMODE_LUMINOUSINTENSITY = 2;
/**
* lux (lm/m^2)
*/
LightConstants.INTENSITYMODE_ILLUMINANCE = 3;
/**
* nit (cd/m^2)
*/
LightConstants.INTENSITYMODE_LUMINANCE = 4;
/**
* Light type const id of the point light.
*/
LightConstants.LIGHTTYPEID_POINTLIGHT = 0;
/**
* Light type const id of the directional light.
*/
LightConstants.LIGHTTYPEID_DIRECTIONALLIGHT = 1;
/**
* Light type const id of the spot light.
*/
LightConstants.LIGHTTYPEID_SPOTLIGHT = 2;
/**
* Light type const id of the hemispheric light.
*/
LightConstants.LIGHTTYPEID_HEMISPHERICLIGHT = 3;
/**
* Light type const id of the area light.
*/
LightConstants.LIGHTTYPEID_RECT_AREALIGHT = 4;
/**
* Light type const id of the clustered light.
*/
LightConstants.LIGHTTYPEID_CLUSTERED_CONTAINER = 5;
//#endregion
export { OffsetClipPlaneToRef as a, ResetMatrixFunctions as c, GetOffsetTransformMatrices as i, FloatingOriginCurrentScene as n, OffsetViewToRef as o, GetFullOffsetViewProjectionToRef as r, OverrideMatrixFunctions as s, LightConstants as t };
