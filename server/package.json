{
  "name": "@battleprompt/game-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "PYTHONPATH=.. $(cd ../.. && pwd)/.venv/bin/python -m compileall -q battleprompt_game",
    "check": "PYTHONPATH=.. $(cd ../.. && pwd)/.venv/bin/python -m compileall -q battleprompt_game",
    "test": "PYTHONPATH=.. $(cd ../.. && pwd)/.venv/bin/python -m pytest -q python_tests"
  }
}
